import { KmFormatterPipe } from './km-formatter.pipe';

describe('KmFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new KmFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
